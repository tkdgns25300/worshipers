# dependencies / build (future scaffold)
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# env
.env*
!.env.example

# design — standalone HTML mockups are scratch (keep tokens.css)
design/*.html

# os / editor
.DS_Store
*.log
