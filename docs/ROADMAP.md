# Worshipers — 작업 로드맵 (TODO)

> 페이지 명세는 [`SPEC.md`](./SPEC.md), 데이터 모델은 [`DATA.md`](./DATA.md), 환경·재개는 [`../README.md`](../README.md).
> 브랜치: `main`(배포) / `dev`(작업), default `main`. commit·push·merge는 사용자 명시 요청 시에만. merge 커밋 X (fast-forward).

## Phase 0: 프로젝트 준비

### 기획 (완료)
- [x] 요구사항 정리 + 핵심 결정 (데이터=수동 큐레이션, 수익=후원 중심·광고 안 함, 범위=일정·장소·등록 중심)
- [x] 문서 작성 — CLAUDE.md, README.md, docs/{SPEC,DATA,ROADMAP,SNAPSHOT}.md
- [x] `dev` 브랜치 생성

### 인프라 (예정)
- [x] 도메인 확정 — `worshipers.life`
- [ ] 1차 시드 팀 확정 (사용자 목록 제공 대기)
- [ ] 도메인 구매 + Vercel 연결
- [x] 디자인 확정 — claude design (round-2 반영), 토큰 `design/tokens.css`
- [x] Next.js 16 + React 19 + Tailwind v4 + TypeScript strict 셋업 (빌드·린트 통과, plain SSG)
- [x] shadcn 설정 (`components.json`·`lib/utils` cn) — 기본 컴포넌트는 step 3에서
- [x] 토큰 이식 — `design/tokens.css` → `globals.css @theme` (팔레트 3종×라이트/다크)
- [x] `.gitignore`·`.env.example`·`next.config.ts`·`tsconfig.json`·postcss·eslint
- [x] GitHub repo + push (default `main`)
- [ ] Vercel 연결 + 첫 배포 (production = `main`)

## Phase 1: MVP — 일정·장소·등록 중심

> 각 페이지 동작은 SPEC.md, 타입·파일 규칙은 DATA.md.

### 1-1. 공통 골격
- [x] 도메인 타입 (`types/domain.ts`) — `Team`·`Gathering`·`Region`·`GatheringCategory`·`GatheringStatus`
- [x] 상수 (`constants/`) — `REGIONS`·카테고리·`SITE`(문의·후원)
- [x] `lib/gathering-status.ts` — `todayKst`·`daysUntil`·상태 파생 (KST 단일 정의)
- [x] `lib/queries.ts` — 정렬·날짜 버킷 그룹·팀 join (속성 필터는 step 3 client)
- [x] 샘플 데이터 레이어 (`data/**` 3팀 + 집회 7) — 실제 시드는 1-2
- [x] 레이아웃 셸 (헤더·푸터·모바일탭) + 공통 컴포넌트(워드마크·상태배지·카테고리태그·집회카드·광고슬롯·후원버튼) — 3a
- [ ] `lib/seo.ts` — JSON-LD·메타 헬퍼 (step 3c)
- [ ] 홈/상세/팀/about/privacy 페이지 (step 3b~3d) + sitemap/robots

### 1-2. 데이터 (시드) — 거의 완료
- [x] **10팀 입력** — 마커스·제이어스·어노인팅·위러브·예수전도단·아이자야씩스티원·잔치공동체·피아워십·예람워십·팀룩워십 (Team + 다가오는 집회, `sourceUrl` 포함)
- [x] **마커스워십 = 기준 완성팀** — 공식 자료로 전수 검수·확충(온라인 실황·Facebook·소개 2문단·교통/주차/유아실 참석안내·8월말까지 주간 연장). 나머지 팀은 이 형태를 템플릿으로 (휴회 주는 사용자가 별도 입력)
- [ ] 남은 팀 — 기프티드·브리지임팩트·히즈윌·아가파오워십·웨이홈·키퍼스워십: **현재 정기예배 없음** → 자료 받는 대로 team-only 등록, 집회는 공지 뜨면 추가
- [ ] 입력 데이터 전수 검수 — 1-9에서 일괄 (스코프: 예배 모임·찬양집회만, 훈련학교·세미나 제외)
- [ ] 팀 이미지 (직접 제작·허가분만)

### 1-3. 홈 — 집회 목록 (`/`) — 완료 (3b)
- [x] 다가오는 집회 카드 리스트 (날짜순)
- [x] 필터바 (카테고리·팀·지역·기간·무료) + 검색(클라이언트) — URL params 유지
- [x] 상태 배지 + 빈 상태 + "지난 모임 보기" 토글
- [x] 상태/다가오는 분기는 클라이언트 KST 계산 (ISR 미사용)

### 1-4. 집회 상세 (`/gatherings/[id]`) — 완료 (3c)
- [x] `generateStaticParams` 전수 생성 (`dynamicParams=false`)
- [x] 핵심 정보 블록 (일시·장소·입장·등록·출처) + 상태별 CTA·공유
- [x] 주최 팀 카드 링크
- [x] `Event` JSON-LD + OG

### 1-5. 팀 디렉토리·상세 (`/teams`, `/teams/[id]`)
- [x] 팀 카드 그리드 + 검색 (`/teams` 디렉토리) + 내비 "찬양팀"
- [x] 팀 상세 (소개·링크) + 그 팀 모임 (3c) — 1-8에서 재설계(정보 카드 + 컴팩트 행 + 지난 집회). 대표곡 필드는 채택 안 함(커버곡 정확성·원작 존중)
- [x] 디렉토리 카드 재설계 — 콤팩트(설명 제거) + **다음 모임 노출** + 다가오는 팀 우선 정렬 + "비정기 모임" 폴백 (`teams-view.tsx`)

### 1-6. 부가 페이지 — 완료 (3d)
- [x] `/about` (소개·문의 mailto·후원·정정/삭제 요청 창구)
- [x] `/privacy` (GA 쿠키 고지 — **광고 없음 명시**)

### 1-7. SEO·수익·런칭
- [x] `sitemap.ts` · `robots.ts` (3c)
- [ ] GA4 (`NEXT_PUBLIC_GA_ID`) + Search Console 등록
- [x] 후원 버튼 배치 (about·푸터) — 실제 토스 링크는 데이터 단계에서 교체
- [x] **광고 안 함, 후원 중심** — 홈 `AdSlot`·컴포넌트 제거, about/privacy 광고 문구 정리, `.env` 광고 키 제거 완료
- [ ] **후원 수단 확정** — 현재 `SITE.donationUrl`이 `"https://toss.me"` 플레이스홀더(미동작). 실제 토스 송금 링크 + **계좌(은행·계좌번호·예금주)** 표기 추가 → `DonateButton`·about·footer 반영. (대기: 사용자 제공)
- [ ] **수익 모델 재확인** — "후원 중심·광고 없음" 유지 vs 향후 제휴/추천 슬롯(Phase 3) 도입 결정. about·privacy 문구·구조에 영향 → 결정 후 정책 페이지 동기화
- [ ] **소개·정책 페이지 점검** — about(후원·문의·정정/삭제 창구)·privacy(GA 쿠키·"광고 없음" 고지)가 확정 수익 모델·후원 수단과 일치하는지 최종 검토. (참고: `DonateButton`이 `components/ads/`에 위치 — 광고 제거됐으니 폴더 정리 검토)

### 1-8. 정보 보강 "갈 수 있게" + 디자인 — 홈·상세·디렉토리 완료, 잔여 선택

> **목표**: 사용자가 **언제·어디서·누가 + 어떻게 참석**을 한 페이지에서 해결. 순서 = **공통 인프라·디자인 먼저 → 모임·팀별 데이터 → 1-9 검수**.
> 정보 축은 언제/어디서/누가/어떻게(참석). **대상(audience)은 제외하기로 결정.**

**공통 인프라 + 디자인 (1회)**
- [x] 디자인 방향 확정 — **Dawn 팔레트**(near-white + 웜 앰버 `#e07b34`), 토큰 `globals.css`·`design/tokens.css`(`data-palette="dawn"`), favicon/OG = `SITE.brandColor`
- [x] 팀 로고 렌더링 — `TeamAvatar`(`imageUrl`→이미지, 없으면 글자) 카드·상세·host 적용 + favicon/워드마크 마크 통일
- [x] **홈 목록 재설계** — 아젠다 타임라인(주 구분 헤더 `이번 주/다음 주/날짜범위` + 왼쪽 날짜 축) · `lib/queries.ts groupAgendaWeeks(list, today)`
- [x] **카드 재설계** — 예배권 티켓 `AgendaCard`(왼쪽 시간 스텁 + 본문 점선 구분 + 오른쪽 D-day·지역 떼는 스텁 + 양쪽 천공). 모바일은 오른쪽 스텁 숨김
- [x] **필터 재설계** — 카테고리 칩 + 「필터(n)」 패널(팀 검색·지역·기간 프리셋·**날짜 기간 from/to**·무료) + 선택칩 (데스크톱 팝오버/모바일 시트)
- [x] **집회 상세 재설계** — 보딩패스 "입장권"(상태 점 + 천공 정보행 + 케이스별 CTA[등록/다시보기/공식공지]+공유). 목록=타임라인 / 상세=티켓 메타포 통일
- [x] 지도·길찾기 — 주소 → 카카오/네이버 지도 링크 **자동 생성**(`mapUrls`), 장소 줄에 배치 (데이터 추가 0)
- [x] 상세 "참석 안내" 블록 — `note` **라벨형 리스트**(`라벨 — 내용` 줄, 자유 텍스트는 한 단락). 교통·입장·주차·유아실 등
- [x] **집회별 OG 이미지** — `gatherings/[id]/opengraph-image.tsx`(ImageResponse + Pretendard), 카카오 키 불필요. 카톡/SNS 미리보기 카드
- [x] **home-view 분해** — `AgendaTimeline`·`FilterPanel` 컴포넌트 추출 (423→285줄)
- [x] **반복 시리즈 접기** — 팀 페이지에서 정기 반복(제목+장소+시간 동일)을 "매주 N" 배지 + 다음 회차 1줄로 접음. 지난 집회는 반복분 제외. 집회 상세 "다른 모임"도 반복 제외 시리즈 수로 카운트(0이면 숨김)
- [x] **공식 채널 = 브랜드 색 로고** — lucide 라인 → Simple Icons 글리프(YouTube 빨강·Instagram 그라데이션·Facebook 파랑), `src/components/icons/brand-icons.tsx`
- [x] **팀 상세 재설계** — 팀이 주어: 무채색 헤더 + **정보 카드**(정기일정·활동지역 + 공식 채널) + 다가오는 모임 **컴팩트 행 `GatheringRow`** + **지난 집회 접기**. 옛 `GatheringCard`(캘린더 타일) 삭제 — 풀 티켓 `AgendaCard`는 홈·집회상세 전용으로 역할 분리. 미사용 Team 필드 `denomination`·`links.kakao` 제거(DATA.md 반영)

**모임·팀별 데이터 (구조 갖춘 뒤)**
- [ ] `endTime`·입장시각·교통·주차·정원 등 공지에서 채우기
- [ ] 팀 로고 — 10팀 채널 로고 적용됨(176~225px PNG). ⚠️ 정책상 **팀별 허가 확보**가 공개 전 남음(재호스팅 금지). 고해상 공식 로고 받으면 교체

**결정 완료**
- 정보 축: 언제·어디서·누가·어떻게(참석). 대상(audience) 제외.
- 색·디자인: **Dawn 팔레트** + Pretendard(큰 시간만 세리프). 홈 목록 = **아젠다 타임라인 + 예배권 티켓 카드**.
- 로고: 팀별 허가 → 공식 로고 (직접 호스팅은 원칙 위배라 안 함).
- 참석/위치: 새 필드 최소화(지도 자동·기존 `note` 활용), 구조화는 **표시(디자인)** 차원에서.

### 1-9. 데이터 전수 검수 (모든 팀 입력 후) — 결정 보류, 나중에 일괄 확정
> **2026-06-24 내부 감사 완료** (전 10팀 코드 기준) — 아래가 확인된 미결 항목. 대부분 **외부 확인**(공지 URL·생중계 여부·주소)이나 **사용자 결정**(표기 규칙)이 필요해 **지금 확정하지 않는다.** 남은 6팀 입력 + 외부 확인 후 일괄 확정. 관련 위치엔 `TODO(검수)`/`TODO(운영)` 주석을 코드에 남겨둠.

**신뢰도(출처 대조)**
- [ ] **`sourceUrl` 전 팀 공통: 루트 → 각 집회 공지 URL로 교체 (10/10 해당)** — 현재 모두 홈페이지·인스타·블로그 루트
- [ ] markers `isOnline: true`(유튜브 생중계) 사실 확인 — 미확인 시 `isOnline`·`liveUrl`·팀 `regions` '온라인' 제거. (제이어스·어노인팅은 생중계 없음 확인됨)
- [ ] fia 목요예배 온라인 생중계 여부 확인 (현재 미확인이라 `isOnline` 미설정)
- [ ] j-us 1·5월 집회 지역 '경기'(선한목자·거룩한빛광성교회) 검증 + 주소 채우기 (현재 추정값·주소 없음)
- [ ] 장소 주소 누락 보완 — `venue.name`만 있고 `address` 없는 곳(j-us 2곳·ywam 등) → 길찾기 정확도

**완전성(필드 비어있음 — 공지 시 채움)**
- [ ] welove 집회: `venue`·`startTime`·`isFree`·`registration` 비어있음 → 공식 공지 뜨면 채움
- [ ] anointing `endTime` 누락 (종료시간 미확인 — 마커스는 21:30)
- [ ] i6tyone·yeram: 정기예배 없어 집회 0건(team-only) → 공지 뜨면 추가

**형태 일관성(결정 필요)**
- [ ] 팀 표기명 규칙 — "마커스워십" vs "마커스" (접미사 통일 여부)
- [ ] `nameEn` 규칙 — Worship/Ministry 등 팀별 실제 영문명을 그대로 둘지
- [ ] `regularSchedule` 텍스트 포맷 통일 여부 (종료시간 유무 등)

**운영(곧 발생)**
- [ ] 주간 반복 집회 날짜 연장 규칙 — 정기 일정은 분기/학기 단위로 N주 한 번에 입력(staleness 방지). DATA.md 운영전략에 반영
- [ ] markers 7월 이후 목요일 추가 — 현재 06-25 이후 다가오는 모임 0건

**컨벤션(정책)**
- [x] 코드 주석 언어 — **한글 허용으로 결정** (도메인 맥락↑). 식별자·커밋은 영어 유지. CLAUDE.md 반영 완료

> **Phase 1 완료 기준**: 확정 팀들의 다가오는 집회를 검색·필터로 찾고, 상세에서 공식 등록 링크로 이동 가능. 모바일 OK, SEO·후원 동작.

## Phase 2: 고도화 (이용자 생긴 뒤)
- [ ] 셋리스트/곡 정보 (곡명·유튜브 링크. 가사·악보 X)
- [ ] **다중 워십팀 집회 구조 고도화** — 현재 `guestTeamIds`(참여팀 참조)로 1차 처리(주최 1팀 + 참여 디렉터리 팀 교차 노출·링크). 여러 찬양팀이 함께 나오는 연합이 늘어나면 추후 구조화: 공동주최(단일 대표 없는 순수 연합) 표현, 카드에 참여팀 표시, 미등록 팀(예: 키퍼스워십) 등록·처리 규칙, 게스트로 교차 노출될 때 'OO 참여' 배지 등 UX 정리
- [ ] 지도 임베드 / 길찾기
- [ ] 집회 후기·사진
- [ ] 제보 폼(구글폼 등) + 수동 검수 파이프라인
- [ ] 공동 편집 — GitHub PR로 팀·집회 기여 받기 (운영 전략은 DATA.md)
- [ ] 통합 검색 (집회·팀·곡)
- [ ] 다크모드

## Phase 3: 확장
- [ ] 좋아하는 팀 집회 알림 (캘린더 .ics 구독 등 — DB 없이 가능한 방식 우선)
- [ ] 제휴/스폰서 영업 ("추천 집회" 스폰서 슬롯) — 트래픽 확보 후
- [ ] affiliate 커미션 (기독교 도서·굿즈 링크)
- [ ] 데이터 모니터링 — 팀 유튜브 RSS 감지 → 집회 초안 자동 생성(관리자 승인). 전략은 DATA.md
- [ ] 다국어 (i18n)
