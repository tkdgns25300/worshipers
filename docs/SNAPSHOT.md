# Worshipers — 스냅샷 (현재 상태)

> 재개 시 **첫 참조**. "지금 어디까지 됐고, 다음에 뭘 하는가"만. 상세는 SPEC/DATA, 전체 작업은 ROADMAP.

## 시점 (2026-06-11)

- **단계**: Phase 1 화면 전부 완성(홈·집회상세·팀상세·about·privacy + SEO), build 통과. 지금은 **실데이터 채우는 중**.
- **브랜치**: `dev`(작업)·`main`(배포) 동기화. default `main`, merge 커밋 없음(fast-forward).
- **데이터**: **마커스**(목요예배 3)·**제이어스**(정기예배 3) 완료. 나머지 팀은 자료 대기. 14팀 목록은 작업용 `찬양팀.md`(미추적).

## 다음 할 일

1. **데이터 채우기 (진행 중)** — 나머지 팀 + 각 팀 집회. `sourceUrl` 필수, 훈련학교/세미나는 스코프 밖(예배 모임만).
2. **데이터 검수** — 이미 넣은 것 포함 전수 확인: 빠진 내용·잘못된 값·이상한 표기 없는지 공식 출처와 대조 (지역·시간·등록·무료 여부 등).
3. **디자인 레이아웃 재작업** — **팀 소개(팀 상세)·모임 상세** 레이아웃을 UI/UX 관점에서 다시. 실데이터 형태에 맞춰 정보 위계·여백·이미지 유무 재검토.
4. **수익 모델 확정** — 광고 안 함, 후원 중심(미확정). 확정 후 docs 반영.
5. **배포** — Vercel 연결·배포 + 도메인 `worshipers.life` 구매·연결. 이후 GA4·Search Console.

## 확정된 값

- **아키텍처**: DB 없음 · 파일 기반(`src/data/**`) · SSG · Vercel. 로그인·인증·서버액션 없음.
- **데이터**: 관리자 수동 큐레이션(+ Claude 초안). 상태(예정/오늘/등록마감/종료)는 클라이언트 KST 파생(미저장). 재호스팅 금지·링크 아웃.
- **스코프**: 예배 모임(집회)만. 훈련학교·세미나·컨퍼런스는 제외(향후 확장은 별도 결정). 로그인·알림·저장됨 제외, 셋리스트=Phase 2.
- **카테고리 6종**: 정기예배·연합예배·거리예배·수련회·기도모임·절기예배.
- **디자인**: 팔레트 `sanctuary`(인디고+앰버, 기본), Pretendard, 라이트 기본·다크 대응. 토큰=`design/tokens.css`.
- **수익**: 광고 안 함, 후원 중심(미확정).
- **도메인** `worshipers.life` · **문의** `tkdgns25300@naver.com`.

## 대기 중 입력

- 나머지 팀 자료 (공식 채널 일정·장소·등록)
- 토스 송금 링크/QR

## 문서 지도

`CLAUDE.md` HOW · `README.md` 환경 · `docs/SPEC.md` 페이지·기능 · `docs/DATA.md` 데이터 모델·운영 · `docs/ROADMAP.md` 작업 · `docs/DESIGN_BRIEF.md` 디자인 브리프+프롬프트 · `design/tokens.css` 토큰
