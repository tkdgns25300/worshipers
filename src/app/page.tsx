// 임시 홈 — 셸 검증용. 실제 홈(히어로·필터·날짜 버킷)은 step 3b에서.
export default function Home() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="text-xl font-bold text-ink">worshipers</h1>
      <p className="mt-2 text-ink-mute">셸·공통 컴포넌트 셋업 완료. 홈 화면은 다음 단계(3b)에서 구성됩니다.</p>
    </div>
  );
}
